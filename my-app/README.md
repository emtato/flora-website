# Flora Li Real Estate

A responsive one-page website for Flora Li, Broker, MBA, and MCNE. Built with React, TypeScript, and Vite.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

Vite uses relative asset paths, so the generated `dist` folder can be hosted from a GitHub Pages repository subpath.
